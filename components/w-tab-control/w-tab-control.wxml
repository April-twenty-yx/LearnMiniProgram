<!--components/w-tab-control/w-tab-control.wxml-->
<view class="container">
  <view class="item {{ currentIndex == index ? 'active' : '' }}" wx:for="{{ list }}" wx:key="{{ index }}">
    <text bind:tap="handleTap" data-index="{{ index }}" data-item="{{ item }}">{{ item }}</text>
  </view>
</view>
